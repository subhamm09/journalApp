mongodb:
  uri: ${cred}
  port: 27017
  host: localhost
  database: journaldb
  auto-index-creation: true

#will shift the configurations to dev will be used in the default profile.
spring:
  profiles:
    active: dev


